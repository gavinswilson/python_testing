

if __name__ == "__main__":
    import sys
    from pathlib import Path

    # Ensure the script is run from the correct directory
    # current_path = Path(__file__).resolve()
    # if current_path.parent != Path.cwd():
    #     print(f"Please run this script from the directory: {current_path.parent}")
    #     sys.exit(1)
    print("Script is running in the correct directory.")
